<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- javac holds a symbol table for the whole platform classpath; like the
         other compilers here, it is not comfortable in a default per-app heap. -->
    <application android:largeHeap="true" />
</manifest>
